--------------------------------------------------------------------------------
--  Problem 43
--------------------------------------------------------------------------------
--  16695334890
--------------------------------------------------------------------------------

module Problem043 where

import Base
import Juke

solutionFrom [] = solution

solution = sum $
	[ ltoi 10 d
	| d@([d1, d2, d3, d4, d5, d6, d7, d8, d9, d10]) <- permutations [0 .. 9]
	, d1 /= 0
	, d4 `rem` 2 == 0
	, (d3 + d4 + d5) `rem` 3 == 0
	, d6 `rem` 5 == 0
	, (ltoi 10 $ [d5, d6, d7]) `rem` 7 == 0
	, (ltoi 10 $ [d6, d7, d8]) `rem` 11 == 0
	, (ltoi 10 $ [d7, d8, d9]) `rem` 13 == 0
	, (ltoi 10 $ [d8, d9, d10]) `rem` 17 == 0
	]
